name: CI
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Artifact label / release tag (default "draft" publishes a draft prerelease)'
        required: false
        default: 'draft'
        type: string
      upload-artifact:
        description: 'Upload artifacts to a draft prerelease'
        required: true
        default: true
        type: boolean
  pull_request:
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'test.configs/**'
      - '.vscode/'
      - 'appcast.xml'
  push:
    branches:
       - main
       - dev
       - android-fix-action-bug
       - new-design-v2 
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.vscode/'
      - 'appcast.xml'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  run:
    uses: ./.github/workflows/build.yml
    secrets: inherit
    permissions: write-all
    if: "${{!contains(github.event.head_commit.message, 'release: version')}}"
    with:
      upload-artifact: ${{ github.event_name == 'workflow_dispatch' && inputs.upload-artifact || github.event_name == 'push' }}
      tag-name: ${{ inputs.version }}
      
